// ThroughNet app entry. Loads the design-system tokens + chrome, then the
// custom elements. The app shell is <tn-app> (see app-root.ts).
import './styles/tokens.css';
import './styles/app.css';
import './live/tn-live-view';
import './app-root';
